export default function HeroSection() {
  return (
    <div
      id="hero"
      className="flex min-h-screen items-center justify-center bg-stone-800"
    >
      The Hero Section where I introduce myself.
    </div>
  );
}
